<Window x:Class="GraphManager.TaskDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Детали задачи" Height="300" Width="400"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Заголовок:" FontWeight="Bold" Margin="0,0,0,5"/>
        <TextBox Grid.Row="1" Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" 
                 FontSize="14" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="2" Text="Описание (Детали):" FontWeight="Bold" Margin="0,0,0,5" VerticalAlignment="Top"/>
        <TextBox Grid.Row="2" Text="{Binding Details, UpdateSourceTrigger=PropertyChanged}" 
                 AcceptsReturn="True" TextWrapping="Wrap" Height="100" 
                 VerticalScrollBarVisibility="Auto" Margin="0,20,0,0"/>

        <CheckBox Grid.Row="3" Content="Задача выполнена" 
                  IsChecked="{Binding IsCompleted, UpdateSourceTrigger=PropertyChanged}" 
                  Margin="0,10,0,10"/>

        <Button Grid.Row="4" Content="Закрыть" Click="CloseButton_Click" 
                Padding="10,5" HorizontalAlignment="Right"/>
    </Grid>
</Window>